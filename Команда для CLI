jmeter.bat -n -t API_HW_7.jmx -l logfile.log -e -o web -Jvu=100 -Jloop=200 -Jramp-up=10

Где:
• jmeter.bat (это запущенный файл win в каталоге bin, который является основным каталогом jmeter и хранит основной пакет JAR);
• -n - надстройка, которая запускает JM в режиме командной строки;
• -t - надстройка пути к файлу;
• API_HW_7.jmx - сам файл с нагрузкой;
• -l - надстройка пути лог-файла;
• logfile.log - лог-файл;
• -e - надстройка сохранения дашборда после проведения нагрузочного теста;
• -o - надстройка сохранения результатов теста;
• web - папка, где будут хранится результаты теста;
• -Jvu=100 - количество потоков;
• -Jloop=200 - указание, что каждый из потоков прогорится 200 раз;
• -Jramp-up=10 - период, за который произойдёт наплыв пользователей.
